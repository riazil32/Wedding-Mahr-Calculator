# Gemini API Key (Server-side, secured)
# This key is used by the Express server to proxy AI requests
GEMINI_API_KEY=

# Client-side environment variables (Vite convention)
# Only variables prefixed with VITE_ are exposed to the browser
# Note: For Netlify/Next.js, the convention is NEXT_PUBLIC_
VITE_PUBLIC_MAP_KEY=
